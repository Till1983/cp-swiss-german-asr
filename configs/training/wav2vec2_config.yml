model:
  name: facebook/wav2vec2-large-xlsr-53-german

ctc_loss:
  blank_token: "<pad>"
  reduction: mean
  zero_infinity: true

audio_preprocessing:
  sampling_rate: 16000
  normalize: true

feature_extraction:
  feature_type: wav2vec2
  window_size: 25  # milliseconds
  stride: 10       # milliseconds

description: |
  Wav2Vec2 configuration for consistent model initialization and fine-tuning.
  See Section 2 – Core Model Development and Training for details.