# =============================================================================
# RUNPOD CLOUD GPU ENVIRONMENT CONFIGURATION
# =============================================================================
# This file is for RunPod environment setup.
#
# SETUP INSTRUCTIONS:
# 1. SSH into your RunPod instance
# 2. Navigate to project directory: cd /workspace/cp-swiss-german-asr
# 3. Copy this file: cp .env.example.runpod .env
# 4. Update HF_TOKEN with your actual token
# =============================================================================

# Environment selection
ENVIRONMENT=runpod

# =============================================================================
# PATHS - RunPod Network Volume Layout
# =============================================================================
# RunPod uses /workspace with persistent network volume
# Data is stored OUTSIDE the project directory for persistence
#
# Layout:
#   /workspace/
#   ├── cp-swiss-german-asr/    (project code)
#   ├── data/                    (datasets - persistent)
#   ├── models/                  (model checkpoints - persistent)
#   └── results/                 (training results - persistent)

PROJECT_ROOT=/workspace/cp-swiss-german-asr
DATA_DIR=/workspace/data
MODELS_DIR=/workspace/models
RESULTS_DIR=/workspace/results

# =============================================================================
# AUTHENTICATION
# =============================================================================
# HuggingFace token for gated models (required)
# Get your token from: https://huggingface.co/settings/tokens
HF_TOKEN=your_huggingface_token_here

# =============================================================================
# MODEL CONFIGURATION
# =============================================================================
# Use GPU on RunPod
DEVICE=cuda

# =============================================================================
# API CONFIGURATION
# =============================================================================
# API_PORT=8000
# LOG_LEVEL=info

# =============================================================================
# CLOUD GPU CONFIGURATION
# =============================================================================
CLOUD_GPU_PROVIDER=runpod
CLOUD_GPU_TYPE=gpu_type # e.g., rtx3090, a100, etc.

# =============================================================================
# NOTES
# =============================================================================
# - No REMOTE_* variables needed (you're already on RunPod)
# - Paths use /workspace for network volume persistence
# - Data survives pod termination (stored on network volume)
# - Code is in /workspace/cp-swiss-german-asr