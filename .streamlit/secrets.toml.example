# WARNING: This is a template file for secrets configuration
# Copy this file to secrets.toml and add your actual values
# NEVER commit secrets.toml to git - it contains sensitive information!

[api]
# Local development URL
backend_url = "http://localhost:8000"

# Production URL format (uncomment and update for deployment):
# backend_url = "https://your-api-domain.com"
