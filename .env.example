## Model configuration
# MODEL_SIZE=large
# DEVICE= # Options: cpu, cuda, cuda:0, cuda:1, etc.

# API configuration
# API_PORT=8000
# LOG_LEVEL=info

## RunPod configuration
REMOTE_USER=root
REMOTE_HOST=your-pod-id.runpod.io
REMOTE_PORT=22 # SSH port (default: 22)
REMOTE_DIR=/workspace/data

# HuggingFace Authentication (required for gated models)
HF_TOKEN=your_huggingface_token_here

## Cloud GPU Configuration (for scripts)
CLOUD_GPU_PROVIDER=your_cloud_provider_here
CLOUD_GPU_TYPE=your_cloud_gpu_type_here

## Cloud GPU
# CLOUD_GPU_KEY=your_cloud_gpu_key_here
# CLOUD_GPU_ENDPOINT=your_cloud_gpu_endpoint_here

## PATHS (optional, defaults will be used if not set)
PROJECT_ROOT=/app
DATA_DIR=/app/data
MODELS_DIR=/app/models
RESULTS_DIR=/app/results